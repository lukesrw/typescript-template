{
    "main": "./dist/src/index",
    "scripts": {
        "clean": "node dist/clean",
        "build": "npm run clean && tsc && node dist/src/dist",
        "test": "npm run build && mocha dist/src/test"
    },
    "devDependencies": {
        "@types/node": "^14.0.23",
        "chai": "^4.2.0",
        "mocha": "^8.0.1"
    }
}
